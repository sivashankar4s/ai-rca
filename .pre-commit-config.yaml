# pre-commit hooks for ai-rca
# Install:  pip install pre-commit && pre-commit install --hook-type pre-push
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: pytest
        name: pytest (pre-push)
        entry: pytest --cov --cov-fail-under=100
        language: system
        types: [python]
        pass_filenames: false
        stages: [pre-push]
